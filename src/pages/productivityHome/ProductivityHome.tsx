import React, { FC } from "react";

const ProductivityHome: FC = () => {

    console.log("render")

    return (
        <div>
            ProductivityHome
        </div>
    );
};

export default React.memo(ProductivityHome);